{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set granted to the main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "native-player:default"
  ]
}
