"$schema" = "schemas/schema.json"

[default]
description = """
Default permission set — allows the JS layer to launch the
fullscreen native player AND manage inline preview surfaces. Both
are required for the standard app flow (Live hero preview + tap-
to-fullscreen on movies / series / live).
"""
permissions = ["allow-start-player", "allow-inline-preview"]
